import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  filter(event) {
    // Allow the default link behavior to occur
    // This controller can be extended in the future for client-side filtering
  }
}
